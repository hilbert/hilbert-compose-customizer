from setuptools import setup

setup(name='hilbert-compose-customizer',
      version='0.1.3',
      description='GPU enabled docker-compose.override generator',
      url='',
      author='',
      author_email='',
      license='Apache 2',
      packages=[],
      install_requires=['pyyaml'],
      scripts=['bin/hilbert-compose-customizer']
      )
